// Check if DB is ready
typeof window.DB, window.DB ? DB.DESTINATIONS.length : 'DB not loaded'

// Check if button is ready
document.getElementById('addMoreEvents')

// Try creating event manually
window.createEventEntry()